{{#if (is "object" properties)}}
{{#each properties}}
{{> t_comments this}}
{{@key}}{{#unless (includes @key (or ../required (array)))}}?{{/unless}}: {{> t_shape this}}
{{/each}}
{{~/if}}
{{#if additionalProperties}}
{{> t_comments additionalProperties}}
[key: string]: undefined | {{#if (is "boolean" additionalProperties)}}any{{else}}{{> t_shape additionalProperties}}{{/if}}
{{/if~}}
