{{~#if (or schema.nullable (not (or (equals schema.type "object") (is "object" schema.properties) schema.additionalProperties))) }}
{{#if export}}export {{/if}}type {{name}} = {{> t_shape schema}}
{{else}}
{{!-- only pure object --}}
{{#if export}}export {{/if}}interface {{name}} {{> t_shape schema}}
{{/if}}
