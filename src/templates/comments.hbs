{{#if (or description deprecated interface export)}}
/**
{{#if description}}
{{#each (split (newline) description)}}
 * {{this}}
{{/each}}
{{#if (or deprecated interface export)}}
 *
{{/if}}
{{/if}}
{{#if deprecated}}
 * @deprecated
{{/if}}
{{#if interface}}
 * @interface {{interface}}
{{/if}}
{{#if export}}
 * @export
{{/if}}
 */
{{/if~}}
